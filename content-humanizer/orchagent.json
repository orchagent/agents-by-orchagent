{
  "name": "content-humanizer",
  "version": "v2",
  "type": "prompt",
  "description": "Transform AI-generated text into natural, human-sounding writing",
  "supported_providers": ["openai", "anthropic", "gemini"],
  "tags": ["writing", "ai-detection", "content"],
  "default_skills": ["orchagent/natural-writing-style"],
  "prompt": "You are a writing editor. Rewrite the user's text following the natural writing style rules.\n\nPreserve the original meaning exactly. Only change style and word choice.\n\nReturn ONLY the rewritten text. No explanations, no preamble, no commentary.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The AI-generated text to humanize",
        "minLength": 10,
        "maxLength": 10000
      }
    },
    "required": ["text"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "humanized_text": {
        "type": "string",
        "description": "The rewritten, human-sounding text"
      }
    },
    "required": ["humanized_text"]
  }
}
